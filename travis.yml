language: go

services:
  - docker

before_install:
- docker pull tomologic/bumpversion
- docker run tomologic/bumpversion v $PWD:/src -w /src tomologic/bumpversion patch

script:
- cat .bumpversion.cfg